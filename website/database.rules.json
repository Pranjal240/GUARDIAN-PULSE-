{
  "rules": {
    ".read": "true",
    ".write": "true",
    "chat_messages": {
      ".indexOn": ["userId", "timestamp"]
    },
    "ecg_readings": {
      ".indexOn": ["userId", "timestamp"]
    },
    "alerts": {
      ".indexOn": ["userId", "status", "createdAt"]
    },
    "users": {
      ".indexOn": ["role", "email"]
    }
  }
}
